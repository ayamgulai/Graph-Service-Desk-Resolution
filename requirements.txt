# ============================================================
# IT Service Desk Knowledge Graph — pip Requirements
# ============================================================

# Core LangChain framework (always required)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-experimental>=0.3.0

# Neo4j Python driver
neo4j>=5.20.0

# Data processing
pandas>=2.0.0

# Environment variable management
python-dotenv>=1.0.0

# ── LLM Provider Packages ─────────────────────────────────────
# Install ONLY the package(s) matching your LLM_PROVIDER setting.

# LLM_PROVIDER=openai   → Cloud: OpenAI GPT-4o / GPT-4
# LLM_PROVIDER=lmstudio → Local: LM Studio (reuses langchain-openai)
langchain-openai>=0.2.0

# LLM_PROVIDER=gemini   → Cloud: Google Gemini
langchain-google-genai>=2.0.0

# LLM_PROVIDER=anthropic → Cloud: Anthropic Claude
langchain-anthropic>=0.3.0

# LLM_PROVIDER=ollama   → Local: Ollama (Llama, Mistral, Gemma, etc.)
langchain-ollama>=0.2.0
